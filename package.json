{
    "name": "patchbay-monorepo",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "npm run build --workspaces"
    },
    "devDependencies": {
        "typescript": "^5.3.3",
        "@types/node": "^20.0.0"
    }
}